import React from 'react'

const Loading = () => {
  return (
    <div className="fixed inset-0 bg-[rgba(0,0,0,.8)] flex items-center justify-center">
    <div className="loader">Loading</div>
    </div>
  )
}

export default Loading